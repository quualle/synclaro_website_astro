---
import '../../styles/global.css';

const title = "KI-Ausbildung für Macher | Synclaro Academy";
const description = "Werde zum KI-Profi: In 6 Monaten lernst du, was andere in Jahren nicht schaffen. Bewirb dich jetzt fürs Gruppen-Coaching.";

const testimonials = [
  { name: 'Sara W.', role: 'Familienbetrieb', quote: 'Nach 50h Coaching habe ich jemanden gefunden, der mich genau da abholt, wo ich stehe.', initials: 'SW' },
  { name: 'Evelyn B.', role: 'Handwerk & Coaching', quote: 'Jetzt kann ich komplexere Tools anwenden und baue sogar eigene Programme.', initials: 'EB' },
  { name: 'Philip K.', role: 'Berater', quote: 'KI-Automatisierungen, die mir täglich Stunden sparen.', initials: 'PK' },
];

const skills = [
  { title: 'Agentic Coding', desc: 'KI schreibt Code für dich', outcome: 'Komplexe Programme ohne Coding-Erfahrung', icon: '01' },
  { title: 'Datenbanken', desc: 'Supabase & PostgreSQL', outcome: 'Eigene Apps mit echten Daten', icon: '02' },
  { title: 'Webseiten', desc: 'Astro, React, Hosting', outcome: 'Professionelle Sites live schalten', icon: '03' },
  { title: 'n8n Workflows', desc: 'Automatisierungen bauen', outcome: 'Stundenlange Arbeit auf Knopfdruck', icon: '04' },
  { title: 'Git & GitHub', desc: 'Versionierung meistern', outcome: 'Code sicher verwalten wie ein Profi', icon: '05' },
  { title: 'Bild & Video', desc: 'Midjourney, Sora & Co.', outcome: 'Einzigartige Inhalte generieren', icon: '06' },
];

const SUPABASE_URL = import.meta.env.PUBLIC_SUPABASE_URL;
const SUPABASE_ANON_KEY = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;
---

<!doctype html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <title>{title}</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <!-- AOS Library -->
  <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <style>
    :root {
      --accent: #f97316;
      --accent-light: #fb923c;
      --accent-dark: #ea580c;
      --dark: #0a0a0a;
      --gray-50: #fafafa;
      --gray-100: #f4f4f5;
      --gray-200: #e4e4e7;
      --gray-400: #a1a1aa;
      --gray-500: #71717a;
      --gray-600: #52525b;
      --gray-800: #27272a;
      --gray-900: #18181b;
    }

    /* Animated Gradient Text */
    .gradient-text {
      background: linear-gradient(135deg, var(--accent) 0%, #fb923c 50%, var(--accent) 100%);
      background-size: 200% auto;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: gradient-shift 3s ease-in-out infinite;
    }

    @keyframes gradient-shift {
      0%, 100% { background-position: 0% center; }
      50% { background-position: 100% center; }
    }

    /* Glass Effects */
    .glass {
      background: rgba(255,255,255,0.8);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
    }

    .glass-dark {
      background: rgba(10,10,10,0.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
    }

    /* Enhanced Card Hover */
    .card-hover {
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }
    .card-hover::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(249,115,22,0.1) 0%, transparent 50%);
      opacity: 0;
      transition: opacity 0.4s;
    }
    .card-hover:hover::before {
      opacity: 1;
    }
    .card-hover:hover {
      transform: translateY(-4px) scale(1.02);
      box-shadow: 0 20px 40px -12px rgba(249,115,22,0.25), 0 0 0 1px rgba(249,115,22,0.1);
    }

    /* Button with Shine Effect */
    .btn-primary {
      background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%);
      position: relative;
      overflow: hidden;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 20px -4px rgba(249,115,22,0.5);
    }
    .btn-primary::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      transition: left 0.5s;
    }
    .btn-primary:hover::before {
      left: 100%;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px -4px rgba(249,115,22,0.6);
    }
    .btn-primary span {
      position: relative;
      z-index: 1;
    }

    /* Accent Line Animation */
    .accent-line {
      width: 48px;
      height: 3px;
      background: linear-gradient(90deg, var(--accent), var(--accent-light));
      position: relative;
      overflow: hidden;
    }
    .accent-line::after {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.8), transparent);
      animation: shine-line 2s ease-in-out infinite;
    }
    @keyframes shine-line {
      0%, 100% { left: -100%; }
      50% { left: 100%; }
    }

    /* Stat Card Hover */
    .stat-card {
      position: relative;
      transition: all 0.3s;
    }
    .stat-card::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--accent), transparent);
      transition: width 0.4s;
    }
    .stat-card:hover::after {
      width: 100%;
    }
    .stat-card:hover {
      transform: scale(1.05);
    }

    /* Pulse Ring */
    @keyframes pulse-ring {
      0% { transform: scale(1); opacity: 1; }
      100% { transform: scale(2); opacity: 0; }
    }
    .pulse-ring::before {
      content: '';
      position: absolute;
      inset: -4px;
      border-radius: 50%;
      border: 2px solid var(--accent);
      animation: pulse-ring 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    /* Floating Animations */
    @keyframes float-1 {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      50% { transform: translateY(-20px) rotate(5deg); }
    }
    @keyframes float-2 {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      50% { transform: translateY(-15px) rotate(-5deg); }
    }
    @keyframes float-3 {
      0%, 100% { transform: translateY(0px) scale(1); }
      50% { transform: translateY(-25px) scale(1.05); }
    }
    .float-1 { animation: float-1 6s ease-in-out infinite; }
    .float-2 { animation: float-2 8s ease-in-out infinite; }
    .float-3 { animation: float-3 7s ease-in-out infinite; }

    /* Blob Animation */
    @keyframes blob {
      0%, 100% { border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; }
      25% { border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%; }
      50% { border-radius: 50% 60% 30% 60% / 30% 60% 70% 40%; }
      75% { border-radius: 60% 40% 60% 30% / 60% 30% 50% 60%; }
    }
    .blob {
      animation: blob 8s ease-in-out infinite;
    }

    /* Gradient Background Animation */
    @keyframes gradient-bg {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    .animated-gradient-bg {
      background: linear-gradient(-45deg, #fff, #fff5eb, #fff, #fef3e6);
      background-size: 400% 400%;
      animation: gradient-bg 15s ease infinite;
    }

    /* Glow Effect */
    .glow-orange {
      box-shadow: 0 0 40px rgba(249,115,22,0.3), 0 0 80px rgba(249,115,22,0.1);
    }

    /* Grid Pattern */
    .grid-pattern {
      background-image:
        linear-gradient(rgba(0,0,0,0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,0,0,0.03) 1px, transparent 1px);
      background-size: 40px 40px;
    }

    /* Scroll Indicator */
    @keyframes scroll-indicator {
      0%, 100% { opacity: 1; transform: translateY(0); }
      50% { opacity: 0.5; transform: translateY(8px); }
    }
    .scroll-indicator {
      animation: scroll-indicator 2s ease-in-out infinite;
    }

    /* Icon Float Container */
    .icon-float {
      position: absolute;
      opacity: 0.1;
      font-size: 2rem;
      color: var(--accent);
    }

    /* Counter Animation */
    .counter {
      display: inline-block;
    }

    /* Magnetic Button Effect */
    .magnetic-btn {
      transition: transform 0.2s ease-out;
    }

    /* Stagger Animation Helper */
    [data-aos] {
      pointer-events: auto !important;
    }

    /* Custom Scroll */
    .scroll-horizontal::-webkit-scrollbar {
      display: none;
    }
    .scroll-horizontal {
      -ms-overflow-style: none;
      scrollbar-width: none;
    }

    /* Parallax Layer */
    .parallax-layer {
      will-change: transform;
    }

    /* Number highlight */
    .number-highlight {
      position: relative;
      display: inline-block;
    }
    .number-highlight::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 30%;
      background: linear-gradient(to top, rgba(249,115,22,0.2), transparent);
      z-index: -1;
    }

    /* Tilt Card */
    .tilt-card {
      transform-style: preserve-3d;
      perspective: 1000px;
    }

    /* Success Animation */
    @keyframes success-pop {
      0% { transform: scale(0.8); opacity: 0; }
      50% { transform: scale(1.05); }
      100% { transform: scale(1); opacity: 1; }
    }
    .success-animation {
      animation: success-pop 0.5s ease-out forwards;
    }

    /* Typewriter cursor */
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
    .cursor-blink::after {
      content: '|';
      animation: blink 1s step-end infinite;
      color: var(--accent);
    }
  </style>
</head>

<body class="bg-white text-gray-900 font-['Inter'] overflow-x-hidden">
  <!-- Floating Background Elements -->
  <div class="fixed inset-0 pointer-events-none overflow-hidden z-0">
    <div class="absolute top-20 left-10 w-72 h-72 bg-orange-500/5 rounded-full blur-3xl float-1"></div>
    <div class="absolute top-1/3 right-10 w-96 h-96 bg-orange-400/5 rounded-full blur-3xl float-2"></div>
    <div class="absolute bottom-20 left-1/4 w-80 h-80 bg-orange-300/5 rounded-full blur-3xl float-3"></div>
  </div>

  <!-- HEADER -->
  <header class="fixed top-0 left-0 right-0 z-50 glass border-b border-gray-200/50">
    <nav class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
      <a href="/" class="text-xl font-black tracking-tight group">
        SYN<span class="gradient-text">C</span>LARO
        <span class="block h-0.5 w-0 group-hover:w-full bg-gradient-to-r from-orange-500 to-orange-300 transition-all duration-300"></span>
      </a>
      <a href="#bewerbung" class="btn-primary text-white px-5 py-2.5 text-sm font-semibold rounded-full magnetic-btn">
        <span>Jetzt bewerben</span>
      </a>
    </nav>
  </header>

  <!-- HERO -->
  <section class="pt-20 pb-16 md:pt-28 md:pb-20 animated-gradient-bg relative overflow-hidden">
    <!-- Animated Grid -->
    <div class="absolute inset-0 grid-pattern opacity-50"></div>

    <!-- Floating Icons -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <div class="icon-float top-32 left-[10%] float-1" style="animation-delay: 0s;">
        <svg class="w-12 h-12" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
      </div>
      <div class="icon-float top-48 right-[15%] float-2" style="animation-delay: 1s;">
        <svg class="w-10 h-10" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
      </div>
      <div class="icon-float top-1/2 left-[5%] float-3" style="animation-delay: 2s;">
        <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24"><path d="M9.4 16.6L4.8 12l4.6-4.6L8 6l-6 6 6 6 1.4-1.4zm5.2 0l4.6-4.6-4.6-4.6L16 6l6 6-6 6-1.4-1.4z"/></svg>
      </div>
      <div class="icon-float bottom-32 right-[8%] float-1" style="animation-delay: 0.5s;">
        <svg class="w-14 h-14" fill="currentColor" viewBox="0 0 24 24"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-5 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/></svg>
      </div>
    </div>

    <div class="max-w-6xl mx-auto px-4 relative z-10">
      <div class="grid md:grid-cols-2 gap-10 md:gap-16 items-center">
        <!-- Text -->
        <div class="order-2 md:order-1" data-aos="fade-right" data-aos-duration="800">
          <div class="inline-flex items-center gap-2 bg-gray-900 text-white px-4 py-1.5 text-xs font-medium uppercase tracking-wider rounded-full mb-6 shadow-lg" data-aos="fade-down" data-aos-delay="200">
            <span class="relative w-2 h-2 bg-orange-500 rounded-full pulse-ring"></span>
            Nächster Start: 12. Januar 2026
          </div>

          <h1 class="text-4xl sm:text-5xl md:text-6xl font-black leading-[1.1] tracking-tight mb-6">
            Werde zum<br/>
            <span class="gradient-text">KI-Profi</span> in
            <span class="number-highlight">6 Monaten</span>
          </h1>

          <p class="text-gray-600 text-lg md:text-xl leading-relaxed mb-8 max-w-lg" data-aos="fade-up" data-aos-delay="300">
            Eigene KI-Tools bauen, automatisieren, umsetzen – was andere in Jahren nicht schaffen.
          </p>

          <!-- Stats with animation -->
          <div class="grid grid-cols-3 gap-6 mb-8" data-aos="fade-up" data-aos-delay="400">
            <div class="stat-card text-center py-4 border-b-2 border-gray-200 cursor-default">
              <div class="text-3xl md:text-4xl font-black gradient-text counter" data-target="6">6</div>
              <div class="text-xs text-gray-500 uppercase tracking-wider mt-1">Monate</div>
            </div>
            <div class="stat-card text-center py-4 border-b-2 border-gray-200 cursor-default">
              <div class="text-3xl md:text-4xl font-black gradient-text counter" data-target="25">25+</div>
              <div class="text-xs text-gray-500 uppercase tracking-wider mt-1">Sessions</div>
            </div>
            <div class="stat-card text-center py-4 border-b-2 border-gray-200 cursor-default">
              <div class="text-3xl md:text-4xl font-black gradient-text counter" data-target="20">20h</div>
              <div class="text-xs text-gray-500 uppercase tracking-wider mt-1">Video</div>
            </div>
          </div>

          <!-- CTAs -->
          <div class="flex flex-col sm:flex-row gap-3" data-aos="fade-up" data-aos-delay="500">
            <a href="#bewerbung" class="btn-primary text-white px-8 py-4 font-semibold text-center rounded-full flex items-center justify-center gap-2 group">
              <span>Jetzt bewerben</span>
              <svg class="w-4 h-4 transition-transform group-hover:translate-x-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
            </a>
            <a href="#ablauf" class="border-2 border-gray-300 px-8 py-4 font-semibold text-center rounded-full hover:border-gray-900 hover:bg-gray-900 hover:text-white transition-all duration-300 hover:shadow-lg">
              Mehr erfahren
            </a>
          </div>

          <p class="mt-6 text-sm text-gray-500" data-aos="fade-up" data-aos-delay="600">
            Geschäftsführer? <a href="/lp/mastermind" class="text-orange-500 hover:text-orange-600 font-medium inline-flex items-center gap-1 group">
              Executive Mastermind
              <svg class="w-3 h-3 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
            </a>
          </p>
        </div>

        <!-- Hero Visual (replaces video) -->
        <div class="order-1 md:order-2" data-aos="fade-left" data-aos-duration="800" data-aos-delay="200">
          <div class="relative">
            <!-- Glowing Background -->
            <div class="absolute inset-0 bg-gradient-to-br from-orange-500/20 via-orange-400/10 to-transparent rounded-3xl blur-2xl transform scale-110"></div>

            <!-- Main Card -->
            <div class="relative bg-gray-900 rounded-3xl p-8 shadow-2xl glow-orange overflow-hidden">
              <!-- Animated Background Pattern -->
              <div class="absolute inset-0 opacity-10">
                <div class="absolute inset-0 bg-gradient-to-br from-orange-500 to-transparent"></div>
                <div class="absolute top-0 left-0 w-full h-full" style="background-image: url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 width=%2260%22 height=%2260%22><rect width=%2260%22 height=%2260%22 fill=%22none%22 stroke=%22%23fff%22 stroke-width=%220.5%22/></svg>');"></div>
              </div>

              <!-- Content -->
              <div class="relative z-10">
                <!-- Terminal Header -->
                <div class="flex items-center gap-2 mb-6">
                  <div class="w-3 h-3 rounded-full bg-red-500"></div>
                  <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
                  <div class="w-3 h-3 rounded-full bg-green-500"></div>
                  <span class="ml-4 text-gray-500 text-xs font-mono">synclaro-academy.ts</span>
                </div>

                <!-- Code Animation -->
                <div class="font-mono text-sm space-y-2">
                  <div class="text-gray-500" data-aos="fade-up" data-aos-delay="400">// Deine KI-Journey startet hier</div>
                  <div data-aos="fade-up" data-aos-delay="500">
                    <span class="text-purple-400">const</span>
                    <span class="text-blue-300">skills</span>
                    <span class="text-white">=</span>
                    <span class="text-orange-400">[</span>
                  </div>
                  <div class="pl-4" data-aos="fade-up" data-aos-delay="600">
                    <span class="text-green-400">"Agentic Coding"</span><span class="text-white">,</span>
                  </div>
                  <div class="pl-4" data-aos="fade-up" data-aos-delay="700">
                    <span class="text-green-400">"Datenbanken"</span><span class="text-white">,</span>
                  </div>
                  <div class="pl-4" data-aos="fade-up" data-aos-delay="800">
                    <span class="text-green-400">"Automatisierung"</span><span class="text-white">,</span>
                  </div>
                  <div class="pl-4" data-aos="fade-up" data-aos-delay="900">
                    <span class="text-green-400">"Web Development"</span>
                  </div>
                  <div data-aos="fade-up" data-aos-delay="1000">
                    <span class="text-orange-400">]</span><span class="text-white">;</span>
                  </div>
                  <div class="mt-4" data-aos="fade-up" data-aos-delay="1100">
                    <span class="text-purple-400">await</span>
                    <span class="text-yellow-300">transform</span><span class="text-white">(</span><span class="text-blue-300">you</span><span class="text-white">);</span>
                    <span class="cursor-blink"></span>
                  </div>
                </div>

                <!-- Progress Indicator -->
                <div class="mt-8 pt-6 border-t border-gray-800">
                  <div class="flex items-center justify-between text-xs text-gray-500 mb-2">
                    <span>Transformation Progress</span>
                    <span class="text-orange-400">100%</span>
                  </div>
                  <div class="h-2 bg-gray-800 rounded-full overflow-hidden">
                    <div class="h-full bg-gradient-to-r from-orange-500 to-orange-400 rounded-full progress-bar" style="width: 0%"></div>
                  </div>
                </div>
              </div>

              <!-- Floating Badge -->
              <div class="absolute -top-3 -right-3 bg-gradient-to-r from-orange-500 to-orange-400 text-white text-xs font-bold px-4 py-2 rounded-full shadow-lg transform rotate-12 float-2">
                Live Coaching
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Social Proof -->
      <div class="mt-16 pt-8 border-t border-gray-200" data-aos="fade-up" data-aos-delay="200">
        <div class="flex flex-wrap items-center justify-center gap-8 md:gap-12">
          {testimonials.map((t, i) => (
            <div class="flex items-center gap-3 group cursor-pointer" data-aos="zoom-in" data-aos-delay={300 + i * 100}>
              <div class="w-12 h-12 bg-gradient-to-br from-orange-500 to-orange-600 rounded-full flex items-center justify-center text-white font-semibold text-sm shadow-lg shadow-orange-500/25 group-hover:scale-110 transition-transform duration-300">
                {t.initials}
              </div>
              <div>
                <span class="font-semibold text-gray-900 text-sm group-hover:text-orange-500 transition-colors">{t.name}</span>
                <span class="text-gray-400 text-sm hidden sm:inline"> · {t.role}</span>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Scroll Indicator -->
      <div class="flex justify-center mt-12">
        <div class="scroll-indicator">
          <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <!-- PROBLEM -->
  <section class="py-16 md:py-24 bg-white relative overflow-hidden">
    <div class="absolute top-0 right-0 w-1/2 h-full bg-gradient-to-l from-orange-50 to-transparent"></div>

    <div class="max-w-6xl mx-auto px-4 relative z-10">
      <div class="text-center mb-12" data-aos="fade-up">
        <div class="accent-line mx-auto mb-4"></div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight">
          KI verändert alles – <span class="gradient-text">aber wie fängst du an?</span>
        </h2>
        <p class="text-gray-600 mt-4 max-w-2xl mx-auto text-lg">
          Die Wahrheit: Um KI wirklich produktiv zu nutzen, brauchst du mehr als nur ChatGPT.
          Du brauchst ein ganzes Ökosystem aus Tools, Workflows und technischem Know-how.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-6 mb-12">
        {[
          { icon: 'M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z', title: 'Überfordert von Tools', desc: 'ChatGPT, Claude, Midjourney, n8n, Cursor, Supabase... Welche brauchst du wirklich – und wie spielen sie zusammen?' },
          { icon: 'M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z', title: 'Keine Zeit für Trial & Error', desc: 'Du hast ein Business. Monate mit YouTube-Tutorials verschwenden ist keine Option.' },
          { icon: 'M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z', title: 'Wissen ≠ Können', desc: 'Du liest viel über KI – aber wenn\'s drauf ankommt, kannst du nichts selbst bauen.' }
        ].map((item, i) => (
          <div class="bg-white p-6 rounded-2xl border border-gray-200 card-hover shadow-sm" data-aos="fade-up" data-aos-delay={i * 100}>
            <div class="w-14 h-14 rounded-2xl bg-gradient-to-br from-orange-500/10 to-orange-500/5 flex items-center justify-center mb-5 group-hover:scale-110 transition-transform">
              <svg class="w-7 h-7 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={item.icon}/></svg>
            </div>
            <h3 class="font-bold text-gray-900 text-lg mb-2">{item.title}</h3>
            <p class="text-gray-600 leading-relaxed">{item.desc}</p>
          </div>
        ))}
      </div>

      <!-- Was wirklich dazugehört -->
      <div class="bg-gray-900 text-white p-8 md:p-10 rounded-3xl relative overflow-hidden" data-aos="fade-up">
        <div class="absolute inset-0 bg-gradient-to-r from-orange-500/20 via-transparent to-orange-500/20"></div>
        <div class="absolute top-0 left-0 w-full h-full opacity-5" style="background-image: url('data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 width=%2260%22 height=%2260%22><rect width=%2260%22 height=%2260%22 fill=%22none%22 stroke=%22%23fff%22 stroke-width=%220.5%22/></svg>');"></div>

        <div class="relative z-10">
          <h3 class="font-bold text-xl mb-6 text-center">Was wirklich dazugehört, um KI produktiv zu nutzen:</h3>
          <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
            {[
              { title: 'Prompting & Agents', desc: 'Fortgeschrittenes Prompting, Agentic Coding mit Claude/Cursor, autonome KI-Workflows' },
              { title: 'Datenbanken', desc: 'Supabase, PostgreSQL, Datenmodellierung, APIs – alles was du für echte Apps brauchst' },
              { title: 'Entwicklung', desc: 'Git, GitHub, Versionierung, Hosting auf Netlify/Vercel, Domain-Setup' },
              { title: 'Automatisierung', desc: 'n8n Workflows, Webhooks, API-Integrationen, Background Jobs' }
            ].map((item, i) => (
              <div class="bg-white/5 p-5 rounded-2xl border border-white/10 hover:bg-white/10 hover:border-orange-500/30 transition-all duration-300 cursor-default" data-aos="zoom-in" data-aos-delay={i * 100}>
                <div class="text-orange-400 font-semibold mb-2">{item.title}</div>
                <p class="text-gray-400 text-sm">{item.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- SKILLS -->
  <section id="ablauf" class="py-16 md:py-24 bg-gray-50 relative overflow-hidden">
    <div class="absolute inset-0 grid-pattern opacity-30"></div>

    <div class="max-w-6xl mx-auto px-4 relative z-10">
      <div class="text-center mb-12" data-aos="fade-up">
        <div class="accent-line mx-auto mb-4"></div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight">Was du in 6 Monaten <span class="gradient-text">lernst & kannst</span></h2>
        <p class="text-gray-600 mt-4 max-w-2xl mx-auto text-lg">Theorie UND Praxis – du baust echte Projekte, die du sofort einsetzen kannst.</p>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
        {skills.map((skill, i) => (
          <div class="bg-white p-6 rounded-2xl border border-gray-200 card-hover group" data-aos="fade-up" data-aos-delay={i * 50}>
            <div class="flex items-start gap-4">
              <div class="w-14 h-14 flex-shrink-0 rounded-2xl bg-gradient-to-br from-gray-100 to-gray-50 flex items-center justify-center group-hover:from-orange-500 group-hover:to-orange-600 transition-all duration-300 shadow-sm">
                <span class="text-xl font-black text-gray-300 group-hover:text-white transition-colors">{skill.icon}</span>
              </div>
              <div>
                <h3 class="font-bold text-gray-900 text-lg">{skill.title}</h3>
                <p class="text-sm text-gray-500">{skill.desc}</p>
                <div class="flex items-center gap-2 mt-3">
                  <svg class="w-4 h-4 text-orange-500" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/></svg>
                  <p class="text-sm text-orange-600 font-medium">{skill.outcome}</p>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Was du danach KANNST -->
      <div class="mt-16" data-aos="fade-up">
        <h3 class="text-2xl font-bold text-center mb-8">Was du danach <span class="gradient-text">wirklich kannst</span>:</h3>
        <div class="grid md:grid-cols-2 gap-5">
          {[
            { title: 'Eigene Web-Apps bauen', desc: 'Von der Idee zur fertigen App mit Datenbank, Authentifizierung und Hosting – ohne klassisches Programmieren.' },
            { title: 'KI-Agents für dich arbeiten lassen', desc: 'Claude Code, Cursor und andere KI-Assistenten so nutzen, dass sie komplexe Aufgaben autonom erledigen.' },
            { title: 'Prozesse komplett automatisieren', desc: 'n8n-Workflows bauen, die E-Mails, CRM, Social Media und mehr miteinander verbinden.' },
            { title: 'Technisch unabhängig sein', desc: 'GitHub, Hosting, Domains – du verstehst das ganze Ökosystem und brauchst keinen teuren Entwickler mehr.' }
          ].map((item, i) => (
            <div class="bg-white p-6 rounded-2xl border border-gray-200 flex items-start gap-4 card-hover" data-aos="zoom-in" data-aos-delay={i * 100}>
              <div class="w-12 h-12 flex-shrink-0 rounded-xl bg-gradient-to-br from-green-500/20 to-green-500/10 flex items-center justify-center">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
              </div>
              <div>
                <h4 class="font-bold text-gray-900 text-lg">{item.title}</h4>
                <p class="text-gray-600 mt-1">{item.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Result -->
      <div class="mt-12 bg-gray-900 text-white p-8 md:p-10 rounded-3xl text-center relative overflow-hidden" data-aos="zoom-in">
        <div class="absolute inset-0 bg-gradient-to-r from-orange-500/30 via-transparent to-orange-500/30"></div>
        <div class="absolute inset-0 blob bg-orange-500/10"></div>
        <div class="relative z-10">
          <p class="text-lg md:text-xl">
            <span class="font-semibold text-orange-400">Das Ergebnis:</span> Du gehörst zu den <span class="font-bold text-white">Top 0,1% der KI-Nutzer</span> – und kannst selbstständig bauen, was andere nur konsumieren.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- ABLAUF -->
  <section class="py-16 md:py-24 bg-white relative">
    <div class="max-w-4xl mx-auto px-4">
      <div class="text-center mb-12" data-aos="fade-up">
        <div class="accent-line mx-auto mb-4"></div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight">So läuft das <span class="gradient-text">Coaching</span> ab</h2>
      </div>

      <div class="space-y-4">
        {[
          { num: '01', title: 'Kick-off & Setup', desc: 'Cursor, Claude, VS Code, GitHub, Supabase – alles einrichten und verstehen' },
          { num: '02', title: 'Wöchentliche Live-Sessions', desc: '25+ Sessions über 6 Monate, plus 20h Videomaterial zum Vertiefen' },
          { num: '03', title: 'Agentic Coding lernen', desc: 'KI-Assistenten Code schreiben lassen, Reviews, Debugging, echte Projekte' },
          { num: '04', title: 'Eigene Projekte bauen', desc: 'Webseiten, Datenbanken, Automatisierungen – live deployen und nutzen' },
        ].map((step, i) => (
          <div class="flex gap-6 items-start p-5 rounded-2xl hover:bg-gray-50 transition-all duration-300 group cursor-default border border-transparent hover:border-gray-200 hover:shadow-lg" data-aos="fade-left" data-aos-delay={i * 100}>
            <div class="w-14 h-14 bg-gray-900 text-white font-bold flex items-center justify-center flex-shrink-0 rounded-2xl group-hover:bg-gradient-to-br group-hover:from-orange-500 group-hover:to-orange-600 transition-all duration-300 shadow-lg">
              {step.num}
            </div>
            <div class="pt-1">
              <h3 class="font-bold text-gray-900 text-lg group-hover:text-orange-500 transition-colors">{step.title}</h3>
              <p class="text-gray-600 mt-1">{step.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FÜR WEN -->
  <section class="py-16 md:py-24 bg-gray-50 relative overflow-hidden">
    <div class="absolute bottom-0 left-0 w-1/2 h-1/2 bg-gradient-to-tr from-orange-50 to-transparent"></div>

    <div class="max-w-4xl mx-auto px-4 relative z-10">
      <div class="text-center mb-12" data-aos="fade-up">
        <div class="accent-line mx-auto mb-4"></div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight">Perfekt für <span class="gradient-text">Macher</span></h2>
      </div>

      <div class="grid md:grid-cols-2 gap-6">
        <div class="bg-white p-8 rounded-3xl border-2 border-gray-900 relative shadow-xl" data-aos="fade-right">
          <div class="absolute -top-4 left-6 bg-gray-900 text-white px-4 py-1.5 rounded-full text-sm font-semibold">Für dich</div>
          <ul class="space-y-4 mt-2">
            {[
              'Selbstständig, Freelancer oder Unternehmer',
              'Du willst <strong>selbst alles können</strong>',
              '4-6 Stunden/Woche Zeit',
              'Echte Skills, nicht nur Theorie'
            ].map((item, i) => (
              <li class="flex items-start gap-3" data-aos="fade-up" data-aos-delay={i * 100}>
                <div class="w-6 h-6 rounded-full bg-green-500/20 flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
                </div>
                <span class="text-gray-700" set:html={item}></span>
              </li>
            ))}
          </ul>
        </div>
        <div class="bg-white p-8 rounded-3xl border border-gray-200 relative" data-aos="fade-left">
          <div class="absolute -top-4 left-6 bg-gray-200 text-gray-600 px-4 py-1.5 rounded-full text-sm font-semibold">Nicht für dich</div>
          <ul class="space-y-4 text-gray-500 mt-2">
            {[
              'Schnelle Lösung ohne Aufwand',
              'Nur verstehen, nicht machen',
              'Keine Zeit fürs Lernen'
            ].map((item, i) => (
              <li class="flex items-start gap-3" data-aos="fade-up" data-aos-delay={i * 100}>
                <div class="w-6 h-6 rounded-full bg-gray-100 flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </div>
                <span>{item}</span>
              </li>
            ))}
          </ul>
          <div class="mt-6 pt-6 border-t border-gray-100">
            <p class="text-sm text-gray-500">Geschäftsführer? <a href="/lp/mastermind" class="text-orange-500 hover:text-orange-600 font-medium">Executive Mastermind →</a></p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- TESTIMONIALS -->
  <section class="py-16 md:py-24 bg-white relative overflow-hidden">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-12" data-aos="fade-up">
        <div class="accent-line mx-auto mb-4"></div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight">Was Teilnehmer <span class="gradient-text">sagen</span></h2>
      </div>

      <div class="flex md:grid md:grid-cols-3 gap-6 overflow-x-auto pb-4 md:pb-0 scroll-horizontal snap-x snap-mandatory">
        {testimonials.map((t, i) => (
          <div class="min-w-[320px] md:min-w-0 bg-gradient-to-br from-gray-50 to-white p-6 rounded-3xl border border-gray-200 snap-start card-hover" data-aos="fade-up" data-aos-delay={i * 100}>
            <div class="flex items-center gap-4 mb-5">
              <div class="w-14 h-14 bg-gradient-to-br from-orange-500 to-orange-600 rounded-full flex items-center justify-center text-white font-bold shadow-lg shadow-orange-500/30">
                {t.initials}
              </div>
              <div>
                <div class="font-bold text-gray-900">{t.name}</div>
                <div class="text-sm text-gray-500">{t.role}</div>
              </div>
            </div>
            <div class="relative">
              <svg class="absolute -top-2 -left-2 w-8 h-8 text-orange-500/20" fill="currentColor" viewBox="0 0 24 24"><path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/></svg>
              <p class="text-gray-600 leading-relaxed pl-4">{t.quote}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- COACH -->
  <section class="py-16 md:py-24 bg-gray-50 relative overflow-hidden">
    <div class="absolute top-0 right-0 w-96 h-96 bg-orange-500/5 rounded-full blur-3xl float-1"></div>

    <div class="max-w-4xl mx-auto px-4 relative z-10">
      <div class="flex flex-col md:flex-row gap-10 items-center bg-white p-8 md:p-10 rounded-3xl border border-gray-200 shadow-xl" data-aos="zoom-in">
        <div class="w-36 h-36 md:w-44 md:h-44 flex-shrink-0 relative">
          <div class="absolute inset-0 bg-gradient-to-br from-orange-500 to-orange-600 rounded-full blur-xl opacity-40 float-2"></div>
          <img src="/images/marco_compressed.jpg" alt="Marco Heer" class="relative w-full h-full object-cover object-top rounded-full ring-4 ring-white shadow-2xl" />
        </div>
        <div class="text-center md:text-left">
          <div class="inline-block text-xs text-orange-500 font-semibold uppercase tracking-wider mb-3 bg-orange-500/10 px-4 py-1.5 rounded-full">Dein Coach</div>
          <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-4">Marco Heer</h2>
          <p class="text-gray-600 mb-6 leading-relaxed text-lg">
            3 Jahre Tag und Nacht KI studiert und angewendet. Hunderte Projekte, tausende Stunden Coaching.
            <strong class="text-gray-900">Praktische Erfahrung</strong>, kein Bullshit.
          </p>
          <a href="#bewerbung" class="inline-flex items-center gap-2 text-orange-500 font-semibold hover:gap-4 transition-all text-lg group">
            Jetzt bewerben
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- BEWERBUNG -->
  <section id="bewerbung" class="py-16 md:py-24 bg-gray-900 text-white relative overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-orange-500/20 via-transparent to-transparent"></div>
    <div class="absolute inset-0 grid-pattern opacity-5"></div>

    <!-- Floating Orbs -->
    <div class="absolute top-20 left-20 w-32 h-32 bg-orange-500/20 rounded-full blur-3xl float-1"></div>
    <div class="absolute bottom-20 right-20 w-40 h-40 bg-orange-400/10 rounded-full blur-3xl float-2"></div>

    <div class="max-w-2xl mx-auto px-4 relative z-10">
      <div class="text-center mb-10" data-aos="fade-up">
        <div class="inline-flex items-center gap-2 bg-gradient-to-r from-orange-500 to-orange-600 text-white px-5 py-2 text-sm font-medium uppercase tracking-wider rounded-full mb-5 shadow-lg shadow-orange-500/30">
          <span class="relative w-2 h-2 bg-white rounded-full pulse-ring"></span>
          Jetzt bewerben
        </div>
        <h2 class="text-3xl md:text-5xl font-black tracking-tight">Bewerbung für die <span class="gradient-text">Academy</span></h2>
        <p class="text-gray-400 mt-4 text-lg">Wir melden uns innerhalb von 48h bei dir.</p>
      </div>

      <form id="application-form" class="bg-white text-gray-900 p-8 md:p-10 rounded-3xl shadow-2xl" data-aos="zoom-in" data-aos-delay="200">
        <input type="hidden" name="program" value="gruppen_coaching" />

        <div class="grid grid-cols-2 gap-4 mb-6">
          <div>
            <label class="block text-xs font-semibold text-gray-600 mb-2 uppercase tracking-wide">Vorname</label>
            <input type="text" name="firstName" required class="w-full px-4 py-3.5 bg-gray-50 border-2 border-gray-200 rounded-xl text-sm focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10 focus:outline-none transition-all" placeholder="Max" />
          </div>
          <div>
            <label class="block text-xs font-semibold text-gray-600 mb-2 uppercase tracking-wide">Nachname</label>
            <input type="text" name="lastName" required class="w-full px-4 py-3.5 bg-gray-50 border-2 border-gray-200 rounded-xl text-sm focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10 focus:outline-none transition-all" placeholder="Mustermann" />
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
          <div>
            <label class="block text-xs font-semibold text-gray-600 mb-2 uppercase tracking-wide">E-Mail</label>
            <input type="email" name="email" required class="w-full px-4 py-3.5 bg-gray-50 border-2 border-gray-200 rounded-xl text-sm focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10 focus:outline-none transition-all" placeholder="max@beispiel.de" />
          </div>
          <div>
            <label class="block text-xs font-semibold text-gray-600 mb-2 uppercase tracking-wide">Telefon</label>
            <input type="tel" name="phone" required class="w-full px-4 py-3.5 bg-gray-50 border-2 border-gray-200 rounded-xl text-sm focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10 focus:outline-none transition-all" placeholder="+49 123 456789" />
          </div>
        </div>

        <div class="space-y-4 mb-6">
          <div class="bg-gray-50 p-5 rounded-2xl border border-gray-100 hover:border-orange-200 transition-colors">
            <p class="text-sm font-semibold text-gray-900 mb-3">Kannst du für die nächsten 6 Monate 4-6 Stunden pro Woche für deine KI-Weiterbildung aufbringen?</p>
            <div class="flex flex-wrap gap-3">
              {['Ja, definitiv', 'Ich muss schauen', 'Eher nicht'].map((opt, i) => (
                <label class="flex items-center gap-2 cursor-pointer text-sm group px-4 py-2 rounded-lg hover:bg-orange-50 transition-colors">
                  <input type="radio" name="q_time" value={['ja', 'vielleicht', 'nein'][i]} class="w-4 h-4 accent-orange-500" />
                  <span class="text-gray-600 group-hover:text-gray-900 transition-colors">{opt}</span>
                </label>
              ))}
            </div>
          </div>
          <div class="bg-gray-50 p-5 rounded-2xl border border-gray-100 hover:border-orange-200 transition-colors">
            <p class="text-sm font-semibold text-gray-900 mb-3">Wie viel Erfahrung hast du bereits mit KI-Tools wie ChatGPT, Claude oder Midjourney?</p>
            <div class="flex flex-wrap gap-3">
              {['Nutze ich regelmäßig', 'Habe ich ausprobiert', 'Kaum bis keine'].map((opt, i) => (
                <label class="flex items-center gap-2 cursor-pointer text-sm group px-4 py-2 rounded-lg hover:bg-orange-50 transition-colors">
                  <input type="radio" name="q_exp" value={['viel', 'etwas', 'wenig'][i]} class="w-4 h-4 accent-orange-500" />
                  <span class="text-gray-600 group-hover:text-gray-900 transition-colors">{opt}</span>
                </label>
              ))}
            </div>
          </div>
          <div class="bg-gray-50 p-5 rounded-2xl border border-gray-100 hover:border-orange-200 transition-colors">
            <p class="text-sm font-semibold text-gray-900 mb-3">Was beschreibt deine berufliche Situation am besten?</p>
            <div class="flex flex-wrap gap-3">
              {['Selbstständig / Freelancer', 'Unternehmer / Geschäftsführer', 'Angestellt'].map((opt, i) => (
                <label class="flex items-center gap-2 cursor-pointer text-sm group px-4 py-2 rounded-lg hover:bg-orange-50 transition-colors">
                  <input type="radio" name="q_status" value={['selbstständig', 'unternehmer', 'angestellt'][i]} class="w-4 h-4 accent-orange-500" />
                  <span class="text-gray-600 group-hover:text-gray-900 transition-colors">{opt}</span>
                </label>
              ))}
            </div>
          </div>
        </div>

        <div class="mb-8">
          <label class="block text-xs font-semibold text-gray-600 mb-2 uppercase tracking-wide">Noch etwas? (optional)</label>
          <textarea name="motivation" rows="3" class="w-full px-4 py-3.5 bg-gray-50 border-2 border-gray-200 rounded-xl text-sm focus:border-orange-500 focus:ring-4 focus:ring-orange-500/10 focus:outline-none transition-all resize-none" placeholder="Was willst du erreichen?"></textarea>
        </div>

        <button type="submit" id="submit-btn" class="w-full btn-primary text-white py-4 font-semibold rounded-xl flex items-center justify-center gap-2 group text-lg">
          <span id="submit-text">Bewerbung absenden</span>
          <span id="submit-loading" class="hidden">Wird gesendet...</span>
          <svg class="w-5 h-5 transition-transform group-hover:translate-x-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
        </button>
        <p class="text-xs text-gray-400 text-center mt-5">
          Mit dem Absenden akzeptierst du unsere <a href="/datenschutz" class="text-orange-500 hover:underline">Datenschutzerklärung</a>
        </p>
      </form>

      <div id="success-message" class="hidden bg-gradient-to-br from-green-500 to-green-600 text-white p-10 rounded-3xl text-center shadow-2xl success-animation">
        <div class="w-20 h-20 mx-auto bg-white/20 rounded-full flex items-center justify-center mb-5">
          <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
        </div>
        <h3 class="text-3xl font-bold mb-3">Bewerbung eingegangen!</h3>
        <p class="text-white/80 text-lg">Wir melden uns innerhalb von 48h.</p>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="bg-gray-900 text-gray-400 py-10 border-t border-gray-800">
    <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row justify-between items-center gap-6 text-sm">
      <a href="/" class="text-white font-black tracking-tight text-xl group">
        SYN<span class="gradient-text">C</span>LARO
      </a>
      <div class="flex gap-8">
        <a href="/impressum" class="hover:text-white transition-colors">Impressum</a>
        <a href="/datenschutz" class="hover:text-white transition-colors">Datenschutz</a>
        <a href="/agb" class="hover:text-white transition-colors">AGB</a>
      </div>
      <span class="text-gray-500">© {new Date().getFullYear()} Synclaro</span>
    </div>
  </footer>

  <!-- AOS Library -->
  <script is:inline src="https://unpkg.com/aos@next/dist/aos.js"></script>

  <!-- SCRIPTS -->
  <script define:vars={{ SUPABASE_URL, SUPABASE_ANON_KEY }}>
    // Initialize AOS
    AOS.init({
      duration: 800,
      easing: 'ease-out-cubic',
      once: true,
      offset: 50,
    });

    // Progress Bar Animation
    setTimeout(() => {
      const progressBar = document.querySelector('.progress-bar');
      if (progressBar) {
        progressBar.style.transition = 'width 2s ease-out';
        progressBar.style.width = '100%';
      }
    }, 1500);

    // Magnetic Button Effect
    document.querySelectorAll('.magnetic-btn').forEach(btn => {
      btn.addEventListener('mousemove', (e) => {
        const rect = btn.getBoundingClientRect();
        const x = e.clientX - rect.left - rect.width / 2;
        const y = e.clientY - rect.top - rect.height / 2;
        btn.style.transform = `translate(${x * 0.1}px, ${y * 0.1}px)`;
      });
      btn.addEventListener('mouseleave', () => {
        btn.style.transform = 'translate(0, 0)';
      });
    });

    // Parallax Effect on Scroll
    let ticking = false;
    window.addEventListener('scroll', () => {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          const scrolled = window.scrollY;
          document.querySelectorAll('.float-1, .float-2, .float-3').forEach((el, i) => {
            const speed = 0.02 + (i * 0.01);
            el.style.transform = `translateY(${scrolled * speed}px)`;
          });
          ticking = false;
        });
        ticking = true;
      }
    });

    // User Behavior Tracking
    const sessionId = crypto.randomUUID();
    const pageLoadTime = Date.now();
    let scrollDepth = 0;
    let interactions = [];

    window.addEventListener('scroll', () => {
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const currentScroll = Math.round((window.scrollY / docHeight) * 100);
      if (currentScroll > scrollDepth) scrollDepth = currentScroll;
    });

    document.addEventListener('click', (e) => {
      const target = e.target.closest('a, button');
      if (target) {
        interactions.push({
          type: 'click',
          element: target.tagName,
          text: target.textContent?.slice(0, 50),
          timestamp: Date.now() - pageLoadTime
        });
      }
    });

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          interactions.push({
            type: 'section_view',
            section: entry.target.id || entry.target.tagName,
            timestamp: Date.now() - pageLoadTime
          });
        }
      });
    }, { threshold: 0.5 });

    document.querySelectorAll('section').forEach(s => observer.observe(s));

    async function sendTracking(eventType) {
      if (!SUPABASE_URL || !SUPABASE_ANON_KEY) return;
      try {
        await fetch(`${SUPABASE_URL}/rest/v1/lp_user_behavior`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'apikey': SUPABASE_ANON_KEY,
            'Authorization': `Bearer ${SUPABASE_ANON_KEY}`
          },
          body: JSON.stringify({
            session_id: sessionId,
            page_path: '/lp/coaching',
            event_type: eventType,
            scroll_depth: scrollDepth,
            time_on_page: Math.round((Date.now() - pageLoadTime) / 1000),
            interactions: interactions.slice(-20),
            device_type: window.innerWidth < 768 ? 'mobile' : 'desktop',
            referrer: document.referrer || null,
            user_agent: navigator.userAgent
          })
        });
      } catch (e) {}
    }

    window.addEventListener('beforeunload', () => sendTracking('page_exit'));
    setTimeout(() => sendTracking('engaged'), 30000);

    // Form
    const form = document.getElementById('application-form');
    const submitBtn = document.getElementById('submit-btn');
    const submitText = document.getElementById('submit-text');
    const submitLoading = document.getElementById('submit-loading');
    const successMessage = document.getElementById('success-message');

    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      submitBtn.disabled = true;
      submitText.classList.add('hidden');
      submitLoading.classList.remove('hidden');

      const formData = new FormData(form);
      const questionnaireAnswers = {};
      ['q_time', 'q_exp', 'q_status'].forEach(key => {
        const val = formData.get(key);
        if (val) questionnaireAnswers[key] = val;
      });

      try {
        const response = await fetch('/api/lp-application', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            firstName: formData.get('firstName'),
            lastName: formData.get('lastName'),
            email: formData.get('email'),
            phone: formData.get('phone'),
            program: formData.get('program'),
            questionnaireAnswers,
            motivation: formData.get('motivation') || null
          })
        });

        if (response.ok) {
          form.classList.add('hidden');
          successMessage.classList.remove('hidden');
          sendTracking('form_submitted');
        } else {
          throw new Error('Server error');
        }
      } catch (error) {
        alert('Fehler aufgetreten. Bitte versuche es erneut.');
        submitBtn.disabled = false;
        submitText.classList.remove('hidden');
        submitLoading.classList.add('hidden');
      }
    });
  </script>
</body>
</html>
