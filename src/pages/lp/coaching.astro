---
import '../../styles/global.css';

const title = "KI-Ausbildung für Macher | Synclaro Academy";
const description = "Werde zum KI-Profi: In 6 Monaten lernst du, was andere in Jahren nicht schaffen. Bewirb dich jetzt fürs Gruppen-Coaching.";

const testimonials = [
  { name: 'Sara W.', role: 'Familienbetrieb', quote: 'Nach 50h Coaching habe ich jemanden gefunden, der mich genau da abholt, wo ich stehe.', initials: 'SW' },
  { name: 'Evelyn B.', role: 'Handwerk & Coaching', quote: 'Jetzt kann ich komplexere Tools anwenden und baue sogar eigene Programme.', initials: 'EB' },
  { name: 'Philip K.', role: 'Berater', quote: 'KI-Automatisierungen, die mir täglich Stunden sparen.', initials: 'PK' },
];

const skills = [
  { icon: '🤖', title: 'KI-Agents', desc: 'Eigene Assistenten bauen' },
  { icon: '⚡', title: 'n8n Workflows', desc: 'Automatisierungen erstellen' },
  { icon: '🌐', title: 'Webseiten', desc: 'Von Idee zur Website' },
  { icon: '🗄️', title: 'Datenbanken', desc: 'Daten strukturieren' },
  { icon: '🎨', title: 'Bild & Video', desc: 'KI-generierte Inhalte' },
  { icon: '💬', title: 'Prompting', desc: 'ChatGPT & Claude meistern' },
];

const SUPABASE_URL = import.meta.env.PUBLIC_SUPABASE_URL;
const SUPABASE_ANON_KEY = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;
---

<!doctype html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <title>{title}</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
</head>

<body class="bg-white text-gray-900 font-sans">
  <!-- STICKY HEADER -->
  <header class="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-sm border-b border-gray-100">
    <nav class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
      <a href="/" class="text-xl font-black">SYN<span class="text-orange-500">C</span>LARO</a>
      <a href="#bewerbung" class="bg-orange-500 text-white px-4 py-2 text-sm font-bold hover:bg-orange-600 transition-colors">
        Jetzt bewerben
      </a>
    </nav>
  </header>

  <!-- HERO - Kompakt & Impactful -->
  <section class="pt-16 pb-8 md:pt-20 md:pb-12 bg-gradient-to-b from-orange-50 to-white">
    <div class="max-w-6xl mx-auto px-4">
      <!-- Mobile: Stack, Desktop: Side by side -->
      <div class="grid md:grid-cols-2 gap-6 md:gap-12 items-center">
        <!-- Text Content -->
        <div class="order-2 md:order-1">
          <div class="inline-flex items-center gap-2 bg-orange-100 text-orange-600 px-3 py-1 text-xs font-bold uppercase tracking-wide mb-4">
            <span class="w-2 h-2 bg-orange-500 rounded-full animate-pulse"></span>
            Nächster Start: Januar 2025
          </div>

          <h1 class="text-3xl sm:text-4xl md:text-5xl font-black leading-tight mb-4">
            Werde zum <span class="text-orange-500">KI-Profi</span> in 6 Monaten
          </h1>

          <p class="text-gray-600 text-base md:text-lg mb-6">
            Eigene KI-Tools bauen, automatisieren, umsetzen. Was andere in Jahren nicht schaffen.
          </p>

          <!-- Stats Row -->
          <div class="grid grid-cols-3 gap-4 mb-6 py-4 border-y border-gray-200">
            <div class="text-center">
              <div class="text-2xl md:text-3xl font-black text-orange-500">6</div>
              <div class="text-xs text-gray-500">Monate</div>
            </div>
            <div class="text-center border-x border-gray-200">
              <div class="text-2xl md:text-3xl font-black text-orange-500">48+</div>
              <div class="text-xs text-gray-500">Sessions</div>
            </div>
            <div class="text-center">
              <div class="text-2xl md:text-3xl font-black text-orange-500">100%</div>
              <div class="text-xs text-gray-500">Hands-on</div>
            </div>
          </div>

          <!-- CTA Buttons -->
          <div class="flex flex-col sm:flex-row gap-3">
            <a href="#bewerbung" class="bg-orange-500 text-white px-6 py-3 font-bold text-center hover:bg-orange-600 transition-colors flex items-center justify-center gap-2">
              Jetzt bewerben
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
            </a>
            <a href="#ablauf" class="border-2 border-gray-300 px-6 py-3 font-bold text-center hover:border-orange-500 hover:text-orange-500 transition-colors">
              Mehr erfahren
            </a>
          </div>

          <!-- GF Breakout -->
          <p class="mt-4 text-xs text-gray-500">
            Geschäftsführer? <a href="/lp/mastermind" class="text-orange-500 hover:underline">→ Executive Mastermind</a>
          </p>
        </div>

        <!-- Video/Image -->
        <div class="order-1 md:order-2">
          <div class="relative aspect-video bg-gray-900 rounded-lg overflow-hidden shadow-2xl">
            <video autoplay muted loop playsinline class="w-full h-full object-cover">
              <source src="/groupcoaching.mp4" type="video/mp4" />
            </video>
            <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent"></div>
            <div class="absolute bottom-3 left-3 right-3">
              <p class="text-white text-sm font-medium">Live aus dem Gruppen-Coaching</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Social Proof Bar -->
      <div class="mt-8 flex flex-wrap items-center justify-center gap-4 md:gap-8 py-4 border-t border-gray-200">
        {testimonials.map((t) => (
          <div class="flex items-center gap-2">
            <div class="w-8 h-8 bg-orange-100 rounded-full flex items-center justify-center text-orange-500 font-bold text-xs">
              {t.initials}
            </div>
            <div class="text-xs">
              <span class="font-medium text-gray-900">{t.name}</span>
              <span class="text-gray-400 hidden sm:inline"> • {t.role}</span>
            </div>
          </div>
        ))}
        <div class="text-xs text-gray-400">+15 weitere Teilnehmer</div>
      </div>
    </div>
  </section>

  <!-- PROBLEM - Horizontal Cards -->
  <section class="py-8 md:py-12 bg-white">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-xl md:text-2xl font-black text-center mb-6">
        KI verändert alles – <span class="text-orange-500">aber wie fängst du an?</span>
      </h2>

      <div class="grid md:grid-cols-3 gap-4">
        <div class="bg-gray-50 p-4 border-l-4 border-orange-500">
          <h3 class="font-bold mb-1">Überfordert von Tools</h3>
          <p class="text-sm text-gray-600">ChatGPT, Claude, Midjourney, n8n... Welche brauchst du wirklich?</p>
        </div>
        <div class="bg-gray-50 p-4 border-l-4 border-orange-500">
          <h3 class="font-bold mb-1">Keine Zeit für Trial & Error</h3>
          <p class="text-sm text-gray-600">Du hast ein Business. Stundenlange Tutorials sind keine Option.</p>
        </div>
        <div class="bg-gray-50 p-4 border-l-4 border-orange-500">
          <h3 class="font-bold mb-1">Wissen ≠ Können</h3>
          <p class="text-sm text-gray-600">Du liest viel – aber kannst nichts selbst bauen.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- SKILLS GRID - Kompakt -->
  <section id="ablauf" class="py-8 md:py-12 bg-gray-50">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-6">
        <h2 class="text-xl md:text-2xl font-black">Was du in 6 Monaten <span class="text-orange-500">lernst</span></h2>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3">
        {skills.map((skill) => (
          <div class="bg-white p-4 text-center border border-gray-200 hover:border-orange-500 hover:shadow-md transition-all">
            <div class="text-2xl mb-2">{skill.icon}</div>
            <h3 class="font-bold text-sm">{skill.title}</h3>
            <p class="text-xs text-gray-500 mt-1">{skill.desc}</p>
          </div>
        ))}
      </div>

      <!-- Result Box -->
      <div class="mt-6 bg-orange-500 text-white p-4 md:p-6 text-center">
        <p class="text-sm md:text-base">
          <span class="font-bold">Ergebnis:</span> Du gehörst zu den <span class="font-bold">Top 0,1% der KI-Nutzer</span> und kannst selbstständig automatisieren, bauen und umsetzen.
        </p>
      </div>
    </div>
  </section>

  <!-- ABLAUF - Timeline -->
  <section class="py-8 md:py-12 bg-white">
    <div class="max-w-4xl mx-auto px-4">
      <h2 class="text-xl md:text-2xl font-black text-center mb-6">So läuft das <span class="text-orange-500">Coaching</span> ab</h2>

      <div class="space-y-4">
        <div class="flex gap-4 items-start">
          <div class="w-10 h-10 bg-orange-500 text-white font-bold flex items-center justify-center flex-shrink-0">01</div>
          <div>
            <h3 class="font-bold">Kick-off & Setup</h3>
            <p class="text-sm text-gray-600">Gemeinsamer Start, Tool-Einrichtung, Entwicklungsumgebung</p>
          </div>
        </div>
        <div class="flex gap-4 items-start">
          <div class="w-10 h-10 bg-orange-500 text-white font-bold flex items-center justify-center flex-shrink-0">02</div>
          <div>
            <h3 class="font-bold">Wöchentliche Sessions</h3>
            <p class="text-sm text-gray-600">6 Monate, 2h/Woche: Agent-Coding, n8n, Webseiten, Hosting</p>
          </div>
        </div>
        <div class="flex gap-4 items-start">
          <div class="w-10 h-10 bg-orange-500 text-white font-bold flex items-center justify-center flex-shrink-0">03</div>
          <div>
            <h3 class="font-bold">Hands-on Projekte</h3>
            <p class="text-sm text-gray-600">Eigene Webseiten, KI-Agents, Automatisierungen, Datenbanken</p>
          </div>
        </div>
        <div class="flex gap-4 items-start">
          <div class="w-10 h-10 bg-orange-500 text-white font-bold flex items-center justify-center flex-shrink-0">04</div>
          <div>
            <h3 class="font-bold">Multimodale KI</h3>
            <p class="text-sm text-gray-600">Bild- & Videogenerierung, neueste KI-Tools für Alltag & Beruf</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FÜR WEN - Side by Side -->
  <section class="py-8 md:py-12 bg-gray-50">
    <div class="max-w-4xl mx-auto px-4">
      <h2 class="text-xl md:text-2xl font-black text-center mb-6">Perfekt für <span class="text-orange-500">Macher</span></h2>

      <div class="grid md:grid-cols-2 gap-4">
        <div class="bg-white p-5 border-2 border-green-500">
          <h3 class="font-bold text-green-600 mb-3 flex items-center gap-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"/></svg>
            Das bist du
          </h3>
          <ul class="space-y-2 text-sm">
            <li>✓ Selbstständig, Freelancer oder Unternehmer</li>
            <li>✓ Du willst <strong>selbst alles können</strong></li>
            <li>✓ 4-6 Stunden/Woche Zeit</li>
            <li>✓ Echte Skills, nicht nur Theorie</li>
          </ul>
        </div>
        <div class="bg-white p-5 border-2 border-gray-300">
          <h3 class="font-bold text-gray-400 mb-3 flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
            Das bist du nicht
          </h3>
          <ul class="space-y-2 text-sm text-gray-500">
            <li>✗ Schnelle Lösung ohne Aufwand</li>
            <li>✗ Nur verstehen, nicht machen</li>
            <li>✗ Keine Zeit fürs Lernen</li>
          </ul>
          <div class="mt-4 pt-3 border-t border-gray-200">
            <p class="text-xs text-gray-500">GF? <a href="/lp/mastermind" class="text-orange-500 hover:underline">→ Executive Mastermind</a></p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- TESTIMONIALS - Horizontal Scroll Mobile -->
  <section class="py-8 md:py-12 bg-white">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-xl md:text-2xl font-black text-center mb-6">Was Teilnehmer <span class="text-orange-500">sagen</span></h2>

      <div class="flex md:grid md:grid-cols-3 gap-4 overflow-x-auto pb-4 md:pb-0 snap-x snap-mandatory">
        {testimonials.map((t) => (
          <div class="min-w-[280px] md:min-w-0 bg-gray-50 p-4 border border-gray-200 snap-start">
            <div class="flex items-center gap-3 mb-3">
              <div class="w-10 h-10 bg-orange-100 rounded-full flex items-center justify-center text-orange-500 font-bold">
                {t.initials}
              </div>
              <div>
                <div class="font-bold text-sm">{t.name}</div>
                <div class="text-xs text-gray-500">{t.role}</div>
              </div>
            </div>
            <p class="text-sm text-gray-600 italic">"{t.quote}"</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- COACH - Kompakt -->
  <section class="py-8 md:py-12 bg-gray-50">
    <div class="max-w-4xl mx-auto px-4">
      <div class="flex flex-col md:flex-row gap-6 items-center">
        <div class="w-32 h-32 md:w-40 md:h-40 flex-shrink-0">
          <img src="/images/marco_compressed.jpg" alt="Marco Heer" class="w-full h-full object-cover rounded-full border-4 border-orange-500" />
        </div>
        <div>
          <div class="text-xs text-orange-500 font-bold uppercase tracking-wide mb-1">Dein Coach</div>
          <h2 class="text-xl md:text-2xl font-black mb-2">Marco Heer</h2>
          <p class="text-sm text-gray-600 mb-3">
            3 Jahre Tag und Nacht KI studiert und angewendet. Hunderte Projekte, tausende Stunden Coaching.
            <strong>Praktische Erfahrung</strong>, kein Bullshit.
          </p>
          <a href="#bewerbung" class="inline-flex items-center gap-2 text-orange-500 font-bold text-sm hover:underline">
            Jetzt bewerben <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- BEWERBUNG - Formular -->
  <section id="bewerbung" class="py-8 md:py-12 bg-gray-900 text-white">
    <div class="max-w-2xl mx-auto px-4">
      <div class="text-center mb-6">
        <div class="inline-flex items-center gap-2 bg-orange-500 text-white px-3 py-1 text-xs font-bold uppercase tracking-wide mb-3">
          <span class="w-2 h-2 bg-white rounded-full animate-pulse"></span>
          Jetzt bewerben
        </div>
        <h2 class="text-2xl md:text-3xl font-black">Bewerbung für die <span class="text-orange-500">Academy</span></h2>
        <p class="text-gray-400 text-sm mt-2">Wir melden uns innerhalb von 48h bei dir.</p>
      </div>

      <form id="application-form" class="bg-white text-gray-900 p-5 md:p-6">
        <input type="hidden" name="program" value="gruppen_coaching" />

        <!-- Name -->
        <div class="grid grid-cols-2 gap-3 mb-4">
          <div>
            <label class="block text-xs font-medium text-gray-600 mb-1">Vorname *</label>
            <input type="text" name="firstName" required class="w-full px-3 py-2 border border-gray-300 text-sm focus:border-orange-500 focus:outline-none" placeholder="Max" />
          </div>
          <div>
            <label class="block text-xs font-medium text-gray-600 mb-1">Nachname *</label>
            <input type="text" name="lastName" required class="w-full px-3 py-2 border border-gray-300 text-sm focus:border-orange-500 focus:outline-none" placeholder="Mustermann" />
          </div>
        </div>

        <!-- Email & Phone -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-3 mb-4">
          <div>
            <label class="block text-xs font-medium text-gray-600 mb-1">E-Mail *</label>
            <input type="email" name="email" required class="w-full px-3 py-2 border border-gray-300 text-sm focus:border-orange-500 focus:outline-none" placeholder="max@beispiel.de" />
          </div>
          <div>
            <label class="block text-xs font-medium text-gray-600 mb-1">Telefon *</label>
            <input type="tel" name="phone" required class="w-full px-3 py-2 border border-gray-300 text-sm focus:border-orange-500 focus:outline-none" placeholder="+49 123 456789" />
          </div>
        </div>

        <!-- Quick Questions -->
        <div class="space-y-3 mb-4">
          <div class="bg-gray-50 p-3">
            <p class="text-sm font-medium mb-2">4-6h/Woche für 6 Monate?</p>
            <div class="flex flex-wrap gap-3 text-sm">
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_time" value="ja" class="accent-orange-500" /> Ja</label>
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_time" value="vielleicht" class="accent-orange-500" /> Vielleicht</label>
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_time" value="nein" class="accent-orange-500" /> Nein</label>
            </div>
          </div>
          <div class="bg-gray-50 p-3">
            <p class="text-sm font-medium mb-2">KI-Erfahrung?</p>
            <div class="flex flex-wrap gap-3 text-sm">
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_exp" value="viel" class="accent-orange-500" /> Viel</label>
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_exp" value="etwas" class="accent-orange-500" /> Etwas</label>
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_exp" value="wenig" class="accent-orange-500" /> Wenig</label>
            </div>
          </div>
          <div class="bg-gray-50 p-3">
            <p class="text-sm font-medium mb-2">Dein Status?</p>
            <div class="flex flex-wrap gap-3 text-sm">
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_status" value="selbststaendig" class="accent-orange-500" /> Selbstständig</label>
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_status" value="unternehmer" class="accent-orange-500" /> Unternehmer</label>
              <label class="flex items-center gap-1 cursor-pointer"><input type="radio" name="q_status" value="angestellt" class="accent-orange-500" /> Angestellt</label>
            </div>
          </div>
        </div>

        <!-- Optional -->
        <div class="mb-4">
          <label class="block text-xs font-medium text-gray-600 mb-1">Noch etwas? (optional)</label>
          <textarea name="motivation" rows="2" class="w-full px-3 py-2 border border-gray-300 text-sm focus:border-orange-500 focus:outline-none resize-none" placeholder="Was willst du erreichen?"></textarea>
        </div>

        <!-- Submit -->
        <button type="submit" id="submit-btn" class="w-full bg-orange-500 text-white py-3 font-bold hover:bg-orange-600 transition-colors flex items-center justify-center gap-2">
          <span id="submit-text">Bewerbung absenden</span>
          <span id="submit-loading" class="hidden">Wird gesendet...</span>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
        </button>
        <p class="text-xs text-gray-400 text-center mt-3">
          <a href="/datenschutz" class="hover:text-orange-500">Datenschutz</a>
        </p>
      </form>

      <!-- Success -->
      <div id="success-message" class="hidden bg-green-500 text-white p-6 text-center">
        <div class="text-3xl mb-2">✓</div>
        <h3 class="text-xl font-bold mb-2">Bewerbung eingegangen!</h3>
        <p class="text-sm">Wir melden uns innerhalb von 48h.</p>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="bg-gray-900 text-gray-400 py-6 border-t border-gray-800">
    <div class="max-w-6xl mx-auto px-4 flex flex-col md:flex-row justify-between items-center gap-4 text-xs">
      <a href="/" class="text-white font-black">SYN<span class="text-orange-500">C</span>LARO</a>
      <div class="flex gap-4">
        <a href="/impressum" class="hover:text-white">Impressum</a>
        <a href="/datenschutz" class="hover:text-white">Datenschutz</a>
        <a href="/agb" class="hover:text-white">AGB</a>
      </div>
      <span>© {new Date().getFullYear()} Synclaro</span>
    </div>
  </footer>

  <!-- TRACKING & FORM SCRIPT -->
  <script define:vars={{ SUPABASE_URL, SUPABASE_ANON_KEY }}>
    // User Behavior Tracking
    const sessionId = crypto.randomUUID();
    const pageLoadTime = Date.now();
    let scrollDepth = 0;
    let interactions = [];

    // Track scroll depth
    window.addEventListener('scroll', () => {
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const currentScroll = Math.round((window.scrollY / docHeight) * 100);
      if (currentScroll > scrollDepth) scrollDepth = currentScroll;
    });

    // Track clicks
    document.addEventListener('click', (e) => {
      const target = e.target.closest('a, button');
      if (target) {
        interactions.push({
          type: 'click',
          element: target.tagName,
          text: target.textContent?.slice(0, 50),
          timestamp: Date.now() - pageLoadTime
        });
      }
    });

    // Track section views
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          interactions.push({
            type: 'section_view',
            section: entry.target.id || entry.target.tagName,
            timestamp: Date.now() - pageLoadTime
          });
        }
      });
    }, { threshold: 0.5 });

    document.querySelectorAll('section').forEach(s => observer.observe(s));

    // Send tracking data
    async function sendTracking(eventType) {
      if (!SUPABASE_URL || !SUPABASE_ANON_KEY) return;

      try {
        await fetch(`${SUPABASE_URL}/rest/v1/lp_user_behavior`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'apikey': SUPABASE_ANON_KEY,
            'Authorization': `Bearer ${SUPABASE_ANON_KEY}`
          },
          body: JSON.stringify({
            session_id: sessionId,
            page_path: '/lp/coaching',
            event_type: eventType,
            scroll_depth: scrollDepth,
            time_on_page: Math.round((Date.now() - pageLoadTime) / 1000),
            interactions: interactions.slice(-20),
            device_type: window.innerWidth < 768 ? 'mobile' : 'desktop',
            referrer: document.referrer || null,
            user_agent: navigator.userAgent
          })
        });
      } catch (e) {
        console.error('Tracking error:', e);
      }
    }

    // Send on page unload
    window.addEventListener('beforeunload', () => sendTracking('page_exit'));

    // Send after 30s
    setTimeout(() => sendTracking('engaged'), 30000);

    // Form submission
    const form = document.getElementById('application-form');
    const submitBtn = document.getElementById('submit-btn');
    const submitText = document.getElementById('submit-text');
    const submitLoading = document.getElementById('submit-loading');
    const successMessage = document.getElementById('success-message');

    form?.addEventListener('submit', async (e) => {
      e.preventDefault();

      submitBtn.disabled = true;
      submitText.classList.add('hidden');
      submitLoading.classList.remove('hidden');

      const formData = new FormData(form);
      const questionnaireAnswers = {};
      ['q_time', 'q_exp', 'q_status'].forEach(key => {
        const val = formData.get(key);
        if (val) questionnaireAnswers[key] = val;
      });

      const data = {
        firstName: formData.get('firstName'),
        lastName: formData.get('lastName'),
        email: formData.get('email'),
        phone: formData.get('phone'),
        program: formData.get('program'),
        questionnaireAnswers,
        motivation: formData.get('motivation') || null
      };

      try {
        const response = await fetch('/api/lp-application', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });

        if (response.ok) {
          form.classList.add('hidden');
          successMessage.classList.remove('hidden');
          sendTracking('form_submitted');
        } else {
          throw new Error('Server error');
        }
      } catch (error) {
        alert('Fehler aufgetreten. Bitte versuche es erneut.');
        submitBtn.disabled = false;
        submitText.classList.remove('hidden');
        submitLoading.classList.add('hidden');
      }
    });
  </script>
</body>
</html>
